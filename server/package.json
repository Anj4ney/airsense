{
  "name": "airsense-server",
  "version": "1.0.0",
  "private": true,
  "description": "AirSense API — lightweight Express proxy for the Groq LLM advisory call (keeps the API key server-side).",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2"
  },
  "engines": {
    "node": ">=18"
  }
}
